## Escopo

Ajustar **apenas** o título principal da seção `HowItWorks` (`src/components/landing/HowItWorks.tsx`), revertendo para o texto original.

## Mudança

Em `src/components/landing/HowItWorks.tsx`, linha 43–47, substituir:

```text
O método do QG para{" "}
<span className="font-display italic font-medium text-gradient-gold">
  transformar perfis em ativos de aquisição
</span>
.
```

Por:

```text
Um processo desenhado para{" "}
<span className="font-display italic font-medium text-gradient-gold">
  escalar com base
</span>
.
```

## Arquivos modificados

```text
src/components/landing/HowItWorks.tsx   (única alteração — 1 bloco de texto)
```

Nenhuma outra seção, token de design, animação ou espaçamento será modificado.